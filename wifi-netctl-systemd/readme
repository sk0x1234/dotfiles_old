#configurations..

#bring wifi interface down..
#  sudo ip link set <INT> down
sudo systemctl disable dhcpcd.service

install all netctl dependecies, 
>>wpa_actiond (needed),
>>dialog, 
>>wpa_supplicant  etc ..   

#connect to a wifi and generate conf...
sudo wifi-menu -o 
sudo netctl-auto enable netctl-auto@wlp2s0.service

disable all netctl(also @).services
